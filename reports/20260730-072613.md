# Rapport de run — `20260730-072613`

| | |
|---|---|
| `run_id` | `20260730-072613` |
| Source primaire | Tennis Warehouse Europe (`tenniswarehouse-europe.com`) |
| Mode | **BOOTSTRAP** — aucun `/data/snapshot_strings.json` ni `/data/snapshot_racquets.json` dans le repo |
| Statut | **`partial`** |
| `abort_reason` | Périmètre restreint à l'entité `strings`, facette multifilament. Facettes polyester / sets poly / hybride / synthetic gut / boyau énumérées (comptages) mais fiches non collectées. Entité `racquets` non énumérée. |
| Disjoncteur déclenché | Aucun |
| Migration | `migrations/20260730-072613__sync.sql` |
| Lignes émises | 52 |

> **Conséquence du mode bootstrap** : aucun passage en `discontinued` n'est autorisé
> (`<phase_0_preflight>`). Aucune opération de cycle de vie n'est émise par ce run.

## Préalable — écart de structure constaté avant toute collecte

Trois constats conditionnent la lecture de tout ce qui suit.

1. **Les tables cibles n'existent pas.** `supabase/schema.sql` ne contient ni `public.strings`
   ni `public.racquets`. Le référentiel réel vit dans `src/data/strings-database.ts` (47 modèles)
   et `src/data/racquets-database.ts` (125 modèles). Le DDL de référence est donc inclus dans la
   migration en `create table if not exists` — additif, sans `ALTER` ni `DROP`.
2. **Le référentiel legacy est inutilisable comme base de diff.** Il ne porte aucun `source_url`
   (0 occurrence dans `strings-database.ts`) ni aucun `generation_label` raquette (0 occurrence
   de `year:`). Sous `<garde_fous_donnees>` l'absence d'URL vaut quarantaine ; sous
   `<cle_naturelle>` une raquette sans libellé de génération n'a pas de clé. Ces données ne sont
   donc **pas** reprises : les reprendre exigerait d'inventer les champs manquants.
3. **Le grain diffère.** Le legacy stocke `gauges: string[]` (une ligne par modèle) ; la clé
   naturelle cible est `(brand_slug, model_slug, gauge_mm)`. Les 47 modèles
   parsables du legacy correspondent à 124 lignes au grain cible.

### Signalement — politique de purge documentée dans le repo

`DATA-AUDIT.md` §0 documente le **retrait de 7 raquettes** (« retirer les modèles introuvables
à l'achat neuf ») : Tecnifibre TFlash 300/285, Völkl V-Cell V1 Pro, Prince Twistpower X100/X105,
ProKennex Ki Q+ Tour Pro / Ki Q+ 5. Base passée de 127 à 120 raquettes.

Cette politique est **incompatible** avec `<regles_cycle_de_vie>` : la bonne primitive est
`lifecycle_status = 'discontinued'`, pas la suppression. Les lignes retirées sont perdues avec
leurs données de compatibilité. Ce run ne les restaure pas — il n'en a pas la source.
À arbitrer humainement avant tout run raquettes.

---

## 1. Compteurs par entité

### Énumération (phase 1) — pagination épuisée

Aucune facette ne présente de lien de pagination : chaque `catpage` expose son listing complet
sur une page unique. Vérifié par absence de `>Next<`, de `class="pagination"` et de paramètre
`?page=` sur les 8 facettes.

| Facette | URL | Items | Fiches collectées |
|---|---|---:|---|
| `MULTIFILSTR` | `/catpage-MULTIFILSTR-EN.html` | 64 | **64 / 64** |
| `ALLMULTIS` | `/catpage-ALLMULTIS-EN.html` | 62 | non — hors périmètre |
| `VALUMULTI` | `/catpage-VALUMULTI-EN.html` | 17 | non — hors périmètre |
| `POLYSTRING` | `/catpage-POLYSTRING-EN.html` | 314 | non — hors périmètre |
| `POLYSTST` | `/catpage-POLYSTST-EN.html` | 42 | non — hors périmètre |
| `HYBRIDSTR` | `/catpage-HYBRIDSTR-EN.html` | 18 | non — hors périmètre |
| `SYNGUTSTR` | `/catpage-SYNGUTSTR-EN.html` | 16 | non — hors périmètre |
| `NATURALGUT` | `/catpage-NATURALGUT-EN.html` | 5 | non — hors périmètre |

**Contrôle d'exhaustivité de la facette prioritaire.** `MULTIFILSTR` (64 SKU) est un
**sur-ensemble** de `ALLMULTIS` (62) et `VALUMULTI` (17) : union = 64 SKU distincts,
`ALLMULTIS \ MULTIFILSTR = 0`. Établi par comparaison d'ensembles de `data-code`, non supposé.
Sans ce contrôle, s'en tenir à une seule des trois facettes aurait sous-compté la priorité.

### Entité `strings` — facette multifilament

| Compteur | Valeur |
|---|---:|
| SKU énumérés | 64 |
| SKU acceptés après normalisation | 61 |
| Lignes au grain clé naturelle | **52** |
| `NEW` | 52 |
| `UPDATED` | 0 |
| `UNCHANGED` | 0 |
| `ABSENT_SOURCE` | 0 |
| Quarantaine | **5** |
| `RECLASS` | 0 (voir §5) |

`NEW` = 100 % : bootstrap, aucun état de référence antérieur. `UPDATED` / `UNCHANGED` ne sont
pas calculables faute de snapshot. Les 61 SKU s'effondrent en 52 lignes car le
coloris n'entre pas dans la clé (cf §5.2).

### Entité `racquets`

| Compteur | Valeur |
|---|---:|
| Énumérés | 0 — **non tenté** |
| Toutes opérations | 0 |

Motif : la clé exige `generation_label`, « valeur publiée par la source, jamais une valeur
inférée ». L'établir demande la collecte fiche à fiche par marque, non réalisée dans ce run.
Émettre des raquettes sans `generation_label` sourcé aurait imposé d'inventer la clé.

> `data/snapshot_racquets.json` n'est **volontairement pas** créé. Un snapshot vide ferait
> perdre le mode bootstrap au prochain run, qui s'autoriserait alors des `discontinued`
> sans historique de comparaison.

---

## 2. Changements de cycle de vie, ligne à ligne

**Aucun.** Zéro opération de statut émise.

| Observation | Lignes | Décision | Fondement |
|---|---:|---|---|
| Présent en source, aucune génération plus récente dans la `family_slug` | 52 | `current`, `absence_streak = 0` | ligne 1 de `<regles_cycle_de_vie>` |
| Présent en source, génération plus récente dans la `family_slug` | 0 | — | non détectable, voir ci-dessous |
| Absent de la source | 1 | **statut inchangé, aucune écriture** | bootstrap |

### Pourquoi aucun `superseded_by` n'est créé

La source ne publie, pour les cordages, **ni regroupement de famille ni libellé de génération**.
`family_slug` est donc posé égal à `model_slug`, ce qui rend par construction indétectable une
génération N+1 dans la même famille.

Deux couples de la collecte **ressemblent** à des générations successives :

| Couple | Preuve |
|---|---|
| `isospeed/control` vs `isospeed/control-classic` | `/ISOSPEED_Control_Classic_130_String/descpageACISOH-ISOCC16-EN.html` |
| `isospeed/professional-new` vs `isospeed/professional-classic` | `/ISOSPEED_Professional_New_120_17_String/descpageACISOH-ISOP17-EN.html` |

« Classic » et « New » suggèrent une antériorité, mais la source ne publie ni millésime ni
relation de remplacement. `<regles_cycle_de_vie>` est explicite : « si la source ne publie pas
de libellé de génération distinctif, […] aucun chaînage n'est créé ». **Ces couples restent
indépendants**, soumis à confirmation humaine.

### Modèle en base absent de la facette source

| Modèle | Jauges legacy | Décision |
|---|---|---|
| `wilson/nxt-soft` | 1.24, 1.30 | `absence_streak` **non incrémenté** |

Justification : bootstrap, donc aucun premier run d'absence opposable. La règle exige de plus
2 runs consécutifs d'absence **ET** absence du site fabricant ; la vérification fabricant n'a
pas été faite (§3.3). Aucune écriture.

---

## 3. Rapport de lacunes multifilament

Sous-couverture confirmée : le référentiel legacy contient **7 modèles**
multifilament sur 47 cordages (14,9 %), là où la seule facette `MULTIFILSTR` en expose
**30**.

### 3.1 Absents du référentiel — 24 modèles

| Marque | Modèle | Jauges (mm) publiées | `source_url` de preuve |
|---|---|---|---|
| `ashaway` | `dynamite-natural` | 1.25 | `/Ashaway_Dynamite_17_125_Natural_String_Optic_Green/descpageACASH-D17SOFT-EN.html` |
| `babolat` | `addixion` | 1.25, 1.3 | `/Babolat_Addixion__125_17_String/descpageACQBA-BADDSP125-EN.html` |
| `babolat` | `xalt` | 1.25, 1.3 | `/Babolat_Xalt_125_17_String/descpageACQBA-BAXAL17-EN.html` |
| `babolat` | `xplore` | 1.25, 1.3, 1.35 | `/Babolat_XPlore_125_17_String_Blue/descpageACQBA-BXPLR25-EN.html` |
| `head` | `rip-control` | 1.25 | `/Head_RIP_Control_125_17_String_Set/descpageACHEAD-RCON17-EN.html` |
| `head` | `velocity-power` | 1.25, 1.3 | `/Head_Velocity_Power_125_17_Black_String/descpageACHEAD-HVP17BK-EN.html` |
| `isospeed` | `control` | 1.3 | `/ISOSPEED_Control_130_16_String/descpageACISOH-ISOC16-EN.html` |
| `isospeed` | `control-classic` | 1.3 | `/ISOSPEED_Control_Classic_130_String/descpageACISOH-ISOCC16-EN.html` |
| `isospeed` | `professional-classic` | 1.2 | `/ISOSPEED_Professional_Classic_120_17_String/descpageACISOH-ISOPC120-EN.html` |
| `isospeed` | `professional-new` | 1.2 | `/ISOSPEED_Professional_New_120_17_String/descpageACISOH-ISOP17-EN.html` |
| `kirschbaum` | `touch-multifiber` | 1.3 | `/Kirschbaum_Touch_Multifiber_130_16_String/descpageACKIRSCHH-TMULTI16-EN.html` |
| `prince` | `premier-control` | 1.4 | `/Prince_Premier_Control_15_140_String/descpageACPRINCEH-PPC15-EN.html` |
| `signum-pro` | `fibercore` | 1.3 | `/Signum_Pro_Fibercore_130_String/descpageACSIGPROH-SPF130S-EN.html` |
| `solinco` | `vanquish` | 1.3 | `/Solinco_Vanquish_130_16_String/descpageACNOASOLH-SVAN16-EN.html` |
| `solinco` | `x-natural` | 1.2, 1.3 | `/Solinco_X-Natural_120_17_String/descpageACNOASOLH-SXN17-EN.html` |
| `tecnifibre` | `duramix-hd` | 1.25, 1.3, 1.35 | `/Tecnifibre_Duramix_HD_125_17_String/descpageACTECNIH-TDM125-EN.html` |
| `tecnifibre` | `nrg2` | 1.24, 1.32 | `/Tecnifibre_NRG2_124_17_String/descpageACTECNIH-NRG17-EN.html` |
| `tecnifibre` | `triax` | 1.28, 1.33, 1.38 | `/Tecnifibre_Triax_128_17_String_/descpageACTECNIH-TSTX28-EN.html` |
| `volkl` | `power-fiber-ii` | 1.25 | `/Volkl_Power_Fiber_II_125_17_String_Natural/descpageACVOLKLGH-VPF217-EN.html` |
| `volkl` | `power-fiber-pro` | 1.25 | `/Volkl_Power_Fiber_Pro_125_17_String_Set/descpageACVOLKLGH-VPF17SS-EN.html` |
| `wilson` | `nxt` | 1.24, 1.3 | `/Wilson_NXT_124_17_String/descpageACWILSON-WNXTS-EN.html` |
| `wilson` | `nxt-power` | 1.26, 1.3 | `/Wilson_NXT_Power_126_17_String/descpageACWILSON-WNXT17-EN.html` |
| `wilson` | `repel` | 1.25, 1.3 | `/Wilson_Repel_17_125_17_String/descpageACWILSON-WRP17NA-EN.html` |
| `wilson` | `sensation` | 1.25, 1.3 | `/Wilson_Sensation_125_17_String/descpageACWILSON-WSNS17-EN.html` |

Aucune de ces lignes n'est comblée par inférence depuis un modèle voisin : chacune provient de
sa propre fiche produit.

### 3.2 Modèles présents des deux côtés — comparaison de jauges

| Modèle | Jauges source | Jauges legacy | Lecture |
|---|---|---|---|
| `babolat/xcel` | 1.25, 1.3 | 1.25, 1.3 | concordant |
| `head/reflex-mlt` | 1.25, 1.3 | 1.25, 1.3 | concordant |
| `head/velocity-mlt` | 1.25, 1.3 | 1.25, 1.3 | concordant |
| `tecnifibre/multifeel` | 1.25, 1.35 | 1.3 | Aucune jauge commune. Le legacy publie 1.30, absent de la facette source. |
| `tecnifibre/tgv` | 1.25, 1.3 | 1.25, 1.3, 1.35 | La 1.35 **existe** en source mais part en quarantaine (construction non publiée). Le legacy n'est pas contredit, il est non corroborable. |
| `tecnifibre/x-one-biphase` | 1.18, 1.24, 1.3 | 1.18, 1.24, 1.3 | concordant (la 1.34/1.35 est en quarantaine, cf §5.1) |

### 3.3 Balayage fabricants — NON RÉALISÉ

La phase 5.2 (couverture de la ligne multifilament de chaque marque sur le site fabricant)
**n'a pas été exécutée**. État des marques du brief dans la facette du distributeur :

| Marque | Rencontrée dans `MULTIFILSTR` |
|---|---|
| Tecnifibre | oui |
| Wilson | oui |
| Head | oui |
| Babolat | oui |
| Yonex | **non** |
| Prince | oui |
| Solinco | oui |
| Luxilon | **non** |
| Gamma | **non** |
| Ashaway | oui |
| Isospeed | oui |
| Kirschbaum | oui |
| Signum Pro | oui |
| Toalson | **non** |
| MSV | **non** |
| Völkl | oui |
| Dunlop | **non** |
| Tourna | **non** |

`Yonex`, `Luxilon`, `Gamma`, `Toalson`, `MSV`, `Dunlop`, `Tourna` n'apparaissent pas dans la facette multifilament.
Deux hypothèses non tranchées : absence réelle de gamme multifilament, ou non-référencement
chez le distributeur. Départager exige le balayage fabricant.
À noter : `MSV` et `TOALSON` sont également **absents de l'index sitemap** du distributeur.

---

## 4. Champs **NON DOCUMENTÉ**

Agrégat sur les 52 lignes émises. Chaque champ listé est `NULL` en base **et** présent dans
`missing_fields`, donc requêtable.

| Champ | Lignes sans valeur | Couverture | Statut |
|---|---:|---:|---|
| `tension_max_lbs` | 52/52 | 0 % | **NON DOCUMENTÉ** |
| `tension_min_lbs` | 52/52 | 0 % | **NON DOCUMENTÉ** |
| `wrap_material` | 52/52 | 0 % | **NON DOCUMENTÉ** |
| `core_material` | 49/52 | 6 % | **NON DOCUMENTÉ** |
| `colors` | 8/52 | 85 % | **NON DOCUMENTÉ** |
| `gauge_label` | 1/52 | 98 % | **NON DOCUMENTÉ** |

### Plages de tension — 0 % de couverture

`tension_min_lbs` / `tension_max_lbs` sont **NON DOCUMENTÉ sur les 52 lignes**. Le
distributeur ne publie pas de plage de tension sur les fiches cordage.

Un cas méritait vigilance car il pouvait produire une fausse valeur : **Ashaway Dynamite**
publie *« should not be strung above 60 pounds […] recommends a 10-20 % lower tension than
standard tennis strings »*. C'est un **plafond** et une consigne relative, pas une plage
min–max. En déduire `tension_max_lbs = 60` puis un minimum par soustraction serait une
extrapolation. Les deux champs restent `NULL`.

### `core_material` / `wrap_material`

`core_material` n'est renseigné que lorsque la source **nomme** le matériau au voisinage du mot
`core` : 3 lignes sur 52 (`Zyex` — Ashaway Dynamite ;
`polyamide` — Babolat Xalt). `wrap_material` : 0/52. Aucune valeur n'est déduite du nom
commercial ni d'un modèle voisin.

### `colors`

8 lignes sur 52 sans coloris publié. Le coloris n'entrant pas dans la
clé, il est agrégé depuis l'ensemble des SKU partageant la clé (§5.2).

---

## 5. Dissensus et anomalies de source

Aucune divergence n'est moyennée ni arbitrée silencieusement.

### 5.1 Contradictions internes à la source primaire → quarantaine

La jauge appartient à la clé naturelle. Une valeur contradictoire ne peut être tranchée sans
fabriquer une clé : ces enregistrements sont rejetés, pas devinés.

| Produit | Titre | Ligne `Gauge` | Motif |
|---|---|---|---|
| Wilson NXT Control | `16 (1.32)` | `16/1.30mm` | `gauge_contradiction_between_title_and_spec_line` |
| Tecnifibre X-One Biphase | `1.34` | `15L/1.35 mm` | `gauge_contradiction_between_title_and_spec_line` |
| Head IntelliTour | `1.25 (17)` | `17 gauge mains (1.23mm)/17 gauge crosses (1.323mm)` | `multi_segment_gauge_...` |

**Wilson NXT Control — seconde anomalie.** La fiche porte `Composition: Natural Gut` alors que
le produit est listé dans la facette multifilament. Très probablement une erreur de saisie du
distributeur. Elle n'est **pas corrigée** : corriger substituerait mon jugement à la source.
Consignée, à arbitrer via le site fabricant en `source_tier = 2`.

**Head IntelliTour — limite du schéma.** La fiche publie deux jauges distinctes
(montants 1.23 mm / travers 1.323 mm) : c'est un set hybride. Deux problèmes cumulés :

1. `gauge_mm numeric(4,2)` ne contient qu'une seule valeur ;
2. `1.323` mm dépasse la précision de `numeric(4,2)` et serait **arrondi silencieusement à
   1.32**, valeur que la source ne publie pas.

### 5.2 Prix divergents entre coloris d'une même clé naturelle

Le coloris n'entre pas dans la clé ; le distributeur crée un SKU **et un prix** par coloris.
Plusieurs SKU s'effondrent donc sur une ligne au prix ambigu.

| Clé naturelle | Prix observés | Retenu | SKU |
|---|---|---:|---|
| `babolat/xplore/1.25` | 7.90 €, 8.90 € | 7.90 € | BXPLR25, BXPLG25 |
| `babolat/xplore/1.3` | 7.90 €, 8.90 € | 7.90 € | BXPLR30, BXPLG30 |
| `head/velocity-power/1.25` | 9.50 €, 9.90 € | 9.50 € | HVP17BK, HVP17NT |
| `head/velocity-power/1.3` | 9.50 €, 9.90 € | 9.50 € | HVP16BK, HVP16NT |

**Convention déclarée** : le prix retenu est le **minimum observé** — seule valeur
déterministe et réellement constatée. La moyenne est exclue par `<garde_fous_donnees>`
(« jamais moyennée »). La divergence est reportée ici plutôt que masquée.

> À arbitrer : si le prix doit être exact par coloris, alors le coloris appartient à la clé
> naturelle — ce que le schéma actuel exclut explicitement.

### 5.3 Construction — ambiguïté `multifilament` vs `multifilament_gaine`

**23 lignes sur 52** portent dans le texte source une mention de revêtement,
gaine ou enrobage. Elles sont classées `multifilament`, **pas** `multifilament_gaine`.

Motif : `<phase_2_normalisation>` réserve `multifilament_gaine` au cas « âme multifilament
+ gaine **polyester** ». Un enrobage polyuréthane ou silicone est présent sur la quasi-totalité
des multifilaments du marché ; l'appliquer basculerait 23 lignes sur 52 vers une
catégorie que la source ne soutient pas. Formulations conservées pour arbitrage :

| Clé | Formulation publiée |
|---|---|
| `ashaway/dynamite-natural/1.25` | Zyex mulitifilament core with a braided wrap… |
| `babolat/xalt/1.25` | Multifilament (polyamide filaments, durable outer wrap with glass beads for greater durability. Silicon coating)… |
| `babolat/xalt/1.3` | Multifilament (polyamide filaments, durable outer wrap with glass beads for greater durability. Silicon coating)… |
| `head/reflex-mlt/1.25` | nfo__section full-width"> Overview With Reflex MLT Head delivers an extremely comfortable multifilament with plenty of… |
| `head/reflex-mlt/1.3` | nfo__section full-width"> Overview With Reflex MLT Head delivers an extremely comfortable multifilament with plenty of… |
| `head/velocity-mlt/1.25` | ct-info__section full-width"> Overview Value hunters looking for an extremely comfortable multifilament with controlla… |
| `head/velocity-mlt/1.3` | ct-info__section full-width"> Overview Value hunters looking for an extremely comfortable multifilament with controlla… |
| `isospeed/control/1.3` | nd is designed to allow the Power Ribbons to move freely within the core. A pre-stretched multifilament layer minimize… |
| `isospeed/professional-new/1.2` | nd is designed to allow the Power Ribbons to move freely within the core. A pre-stretched multifilament layer minimize… |
| `kirschbaum/touch-multifiber/1.3` | gainst abrasion and add a bit more ball bite. Gauge: 16 (1.30mm) Length: 12m Composition: Multifilament synthetic Colo… |

*(23 lignes au total — liste intégrale dans `data/snapshot_strings.json`.)*

Cas le plus net, **Tecnifibre Multifeel** : *« Solid Core (985 Composite multifilaments (60 %
du volume) entourent un Monofilament Central (20 %), cordage immergé dans du Polyuréthane
(20 %) »*. Structure composite réelle. Classé `multifilament` (le mot que la source emploie),
l'énoncé complet étant conservé. Un premier jet de mon classifieur l'avait rangé en
`monofilament` sur la seule présence du mot « Monofilament Central » : corrigé.

### 5.4 Construction non publiée → quarantaine

`construction` est `NOT NULL` : une ligne sans construction sourcée ne peut entrer en table.

| Clé naturelle | Produit | `source_url` |
|---|---|---|
| `head/rip-control/1.3` | Head RIP Control 1.30/16 String Set | `/Head_RIP_Control_130_16_String_Set/descpageACHEAD-RCON16-EN.html` |
| `tecnifibre/tgv/1.35` | Tecnifibre TGV 1.35/15L String | `/Tecnifibre_TGV_135_15L_String/descpageACTECNIH-TTGV15S-EN.html` |

Fait notable : `head/rip-control` et `tecnifibre/tgv` sont **acceptés sur certaines jauges et
rejetés sur d'autres**, la fiche du distributeur étant plus complète pour une jauge que pour
l'autre. La construction connue d'une jauge n'est **pas** recopiée sur la jauge voisine : ce
serait exactement l'extrapolation interdite. Résoluble en `source_tier = 2`.

### 5.5 Champ marque du distributeur contredit le titre produit

7 lignes. Le champ marque mêle entités de distribution et mentions
logistiques ; le titre produit porte la marque commerciale.

| `brand_slug` retenu (titre) | Champ marque source |
|---|---|
| `isospeed` | `IsoSpeed - FeRe` |
| `solinco` | `Noa \Solinco` |

Arbitrage déclaré : le titre fait foi, la divergence est consignée. Sans ce contrôle la base
aurait porté `noa-sports-solinco`, `isospeed-fere-sports-dist` ou `tecnifibre-hardgoods`,
fracturant les clés naturelles par marque.

### 5.6 Noms de champs multiples dans la source

Le distributeur publie le même fait sous plusieurs libellés. Tous sont lus :

| Fait | Clés rencontrées | Pages |
|---|---|---:|
| composition | `composition` / `construction` | 41 / 6 |
| jauge | `gauge` / `string gauge` | 53 / 2 |
| coloris | `colour` / `color` / `colours` / `string color` / `available colors` | 30 / 15 / 2 / 16 / 2 |

Un premier jet ne lisait que `composition`, `gauge`, `colour` et `color` : 6 fiches étaient
classifiées depuis la prose alors qu'un champ structuré existait, et 2 coloris manquaient.
Corrigé après audit de la totalité des clés publiées sur les 64 fiches.

### 5.7 Coquille dans la source

Ashaway Dynamite publie `Composition: Zyex mulitifilament core with a braided wrap`
— *mulitifilament*. Consignée, non corrigée dans la donnée reprise ; la classification vient
de la prose de la même fiche, qui écrit correctement « multifilament core ».

### 5.8 Libellé de modèle ambigu

`Ashaway Dynamite 17 (1.25) Natural String Optic Green` → `model_slug = dynamite-natural`.
« Natural » peut être un qualificatif de gamme ou un coloris. La prose de la même fiche parle
de « Dynamite 17 **Soft** », libellé absent du titre. Le titre est retenu tel que publié ;
la divergence titre/prose est signalée pour arbitrage.

### 5.9 Dissensus source primaire / fabricant

**Aucun mesuré.** Aucun site fabricant n'a été interrogé : toutes les données émises sont en
`source_tier = 1`. Les §5.1, §5.4 et §3.3 énumèrent les points qui *nécessiteraient* une
source secondaire ; ils restent ouverts.

---

## 6. Disjoncteurs évalués

| Disjoncteur | Seuil | Mesuré | Verdict |
|---|---|---|---|
| Catégorie énumérée retournant 0 item | 0 | min = 5 (`NATURALGUT`) sur 8 facettes | **OK** |
| Run modifiant > 15 % des lignes d'une table | 15 % | `public.strings` inexistante, 0 ligne préexistante → dénominateur nul | **non applicable** |
| Run passant > 5 % des lignes en `discontinued` | 5 % | 0 ligne (0 %) | **OK** |
| SQL contenant `DELETE` / `DROP` / `TRUNCATE` / `ALTER` | présence | 0 occurrence hors commentaire — scan automatisé dans `emit_sql.py`, sortie non nulle si détection | **OK** |
| Écart > 30 % du total énuméré vs run précédent | 30 % | aucun run précédent | **non applicable** |

Deux disjoncteurs sont **non applicables**, pas « passés » : ils comparent à un état antérieur
inexistant. Je le déclare plutôt que de les afficher au vert.

Le seuil des 15 % mérite un mot : sur une table vide, 52 insertions représentent 100 % des
lignes. Le disjoncteur vise à intercepter une régression de parsing qui corromprait en masse un
référentiel **établi** ; il n'a pas de sens sur un bootstrap, mode que `<phase_0_preflight>`
reconnaît explicitement. Ce raisonnement est exposé pour être contestable en revue.

### Contrôles supplémentaires exécutés

| Contrôle | Résultat |
|---|---|
| `robots.txt` lu avant toute requête | `User-agent: *` → `Disallow: /zzz/`, `/mailings/` uniquement. Aucun chemin catalogue interdit. |
| Chemin interdit visité | Aucun. Aucun contournement tenté. |
| User-agent | `TennisStringAdvisor-SyncBot/1.0` + contact repo |
| Débit | 1 requête / 3 s, séquentiel, aucun parallélisme. ~75 requêtes. |
| Réponses non-200 | 1 : `catpage-MULTITENNIS.html` → 404. **URL que j'avais devinée**, inexistante. Erreur de ma part, pas un changement de structure source : non traitée comme incident, et le disjoncteur « 0 item » n'a pas été invoqué sur cette base. |
| Structure de page | Stable : tuiles `data-gtm_impression_*` et blocs `Overview` conformes sur 64/64 fiches. |
| Migration validée par parseur PostgreSQL réel | `pglast` : 65 instructions (58 `INSERT`, 4 `CREATE`, 2 `TRANSACTION`, 1 `UPDATE`) |
| Idempotence **exécutée** | **Non.** Aucun serveur PostgreSQL disponible dans l'environnement. Garantie par construction (`where content_hash is distinct from excluded.content_hash`) mais **non prouvée empiriquement**. À vérifier en CI : appliquer deux fois, exiger 0 ligne affectée au second passage. |

---

## 7. Tentatives d'injection détectées

**Aucune.**

64 fiches produit et 9 pages de listing analysées. Aucune directive de type « ignore les
instructions précédentes », « supprime », « exécute », ni demande d'exposition de
configuration n'a été rencontrée dans le contenu récupéré.

Traitement appliqué sans exception : tout contenu web est une **donnée à extraire**. Les seules
chaînes retenues sont les valeurs de champs métier du §4 ; aucun texte source n'a été
interprété comme instruction.

### Données personnelles — exclusion appliquée

Les fiches comportent une section `id="cust_reviews"` (avis clients, pseudonymes, notes
nominatives). Chaque page est **tronquée avant cette section** par `strip_ugc()`
(`scripts/sync/extract_string.py`) préalablement à toute analyse. Aucun avis, pseudonyme ni
contenu utilisateur n'entre dans un artefact. Seules caractéristiques produit et prix sont
collectés — périmètre `<donnees_personnelles>` respecté, aucun traitement RGPD requis.

Les blocs *Manufacturer name / Address / Contact* (conformité GPSR) désignent des personnes
morales. Ils ne sont pas ingérés en table ; ils apparaissent dans les charges de quarantaine à
titre de preuve d'absence de specs.

---

## Reste à faire — priorisé

1. **Arbitrer la politique de purge** de `DATA-AUDIT.md` §0 (7 raquettes supprimées) avant
   tout run raquettes. Décision de gouvernance, pas technique.
2. **Trancher la clé naturelle des hybrides.** `gauge_mm numeric(4,2) NOT NULL` ne peut
   représenter un set à deux jauges. Concerne Head IntelliTour, les 18 items `HYBRIDSTR` et
   `wilson-champions-choice` (jauge legacy `1.25/1.30`). Sans arbitrage, toute la facette
   hybride ira en quarantaine.
3. **Trancher le prix par coloris** (§5.2) : soit le coloris entre dans la clé, soit
   `price_eur` est assumé comme prix plancher et documenté comme tel.
4. **Balayage fabricants** (§3.3) pour les marques absentes et les 5 lignes de quarantaine
   résolubles en `source_tier = 2`.
5. **Facettes restantes** : 314 polyester, 42 sets poly, 18 hybrides, 16 synthetic gut,
   5 boyaux — listings déjà énumérés, fiches à collecter.
6. **Entité raquettes** : énumération par marque, `generation_label` à établir depuis la source.

---

## Demande d'autorisation — code du pipeline hors périmètre d'écriture

`<perimetre_ecriture>` limite l'écriture repo à `/migrations/`, `/data/` et `/reports/`.
Cette PR ne contient donc **que** ces quatre fichiers.

Le code qui a produit ces artefacts (`scripts/sync/` : fetcher à débit plafonné, parseur de
listing, normalisation, émetteurs SQL et rapport, DDL de référence, README) est **délibérément
retenu hors de cette PR** : `scripts/` n'est pas dans le périmètre autorisé, et je n'élargis pas
un périmètre déclaré de ma propre initiative.

Conséquence à assumer : **les artefacts de cette PR ne sont pas reproductibles en l'état.**
C'est un défaut d'auditabilité réel. Les invariants sont décrits en §5 et §6, mais la parole
d'un rapport ne remplace pas du code relisible.

Deux options, au choix du relecteur :

1. autoriser explicitement `scripts/sync/` comme quatrième chemin d'écriture, et j'ouvre une
   PR séparée avec le pipeline ;
2. maintenir le périmètre à trois chemins, et le pipeline reste externe au repo — auquel cas
   la reproductibilité des runs doit être traitée autrement.

Sans arbitrage, je ne committe pas le code.

